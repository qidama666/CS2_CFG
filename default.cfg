// Default Binds
// TODO

// Jump Throw
alias "+jumpaction" "+jump;"
alias "+throwaction" "-attack; -attack2"
alias "-jumpaction" "-jump"
bind "ALT" "+jumpaction;+throwaction;"

// Run Jump Throw
alias "+runthrow" "+forward;+jump;"
alias "-runthrow" "-jump;-forward"
bind "CAPSLOCK" "+runthrow;+throwaction"

// Crouch Jump
alias +duckjump "+jump; +duck";
alias -duckjump "-jump; -duck";
bind "SPACE" "+duckjump"

// Bomb Drop
alias "+dropbomb" "slot3; slot5"
alias "-dropbomb" "drop; lastinv"
bind "Y" +dropbomb

// Lower Volume While Shooting
alias +shoot "+attack; volume 0.7" 
alias -shoot "-attack; volume 1"
bind "MOUSE1" "+shoot"

// Grenades
bind "Z" "slot6; buy hegrenade"
bind "X" "slot8; buy smokegrenade"
bind "C" "slot7; buy flashbang"
bind "V" "slot10; buy molotov;buy incgrenade"

// Radar scale
// TODO (T)

// Lineup Crosshair
// TODO (6)

// Rethrow Grenade
// TODO (FN)

// Kill Smoke & Molly
// TODO (FN)

// Buy Binds
// Rifles
bind "O" "buy famas;"
bind "P" "buy m4a1;"
bind "[" "buy ssg08;"
bind "]" "buy aug;"
bind "\" "buy awp;"
// Pistols
bind "K" "buy elite;"
bind "L" "buy p250;"
bind "semicolon" "buy fn57;"
bind "'" "buy deagle;"
// Mid-tiers
bind "N" "buy nova;"
bind "M" "buy xm1014;"
bind "," "buy mp5sd;"
bind "." "buy p90;"
bind "/" "buy mp9;"
// Equipments
bind "leftarrow" "buy defuser;"
bind "rightarrow" "buy taser;"
bind "downarrow" "buy vest;"
bind "uparrow" "buy vesthelm;buy vest;"
// Full Buy
bind "J" "buy famas;buy hegrenade;buy smokegrenade;buy molotov;buy incgrenade;buy flashbang;buy vesthelm;buy vest;buy defuser;"


// Settings
// TODO
